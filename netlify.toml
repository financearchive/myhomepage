[build]
  publish = "dist"
  command = "npm install && npm run build"
  functions = "src/functions"  # ✅ 여기만 변경됨!

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/404"
  status = 404
